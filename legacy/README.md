# Archived implementations

`egui/` preserves the original standalone Rust application and its lockfile. It is excluded from the active workspace. `complete-rust-fishing-bot.txt` is the original reference snapshot. Neither is built by normal project commands.
